import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — integration tests require a running API.
    expect(true, isTrue);
  });
}
