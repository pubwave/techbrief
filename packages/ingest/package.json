{
  "name": "@techbrief/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@techbrief/shared": "0.1.0",
    "cheerio": "^1.2.0",
    "domhandler": "^5.0.3",
    "fast-xml-parser": "^4.5.0"
  }
}
